spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/UserManagementServer
    username: postgres
    password: Mo7120101038
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
      show_sql: false